# サーバー（mahjong_engine）への依頼事項

Unity クライアント側から見て、追加・修正してほしい情報のまとめ。
2026-08-01 時点。クライアント側の調査で実際に確認した事実だけを書いている。

優先度は **A（不具合・実害あり）→ B（体験の改善）→ C（あると助かる）** の順。

---

## A-1. 存在しない牌IDが送られてくる【バグ】

牌交換（mulligan）の結果、**牌種が範囲外のIDが返ってくる**ことがある。

実際に観測した例:

```
skill_casted の mulliganResult:  old=1  new=63
  63 & 0x1F = 31   → 牌種31は存在しない（有効なのは 0〜28）
```

原因と思われる箇所は `game_engine.py` の配牌処理:

```python
reserved = [t for t in range(116) if t not in used_set]
```

**IDが 0〜115 の連番として扱われているが、実際はビット構造。**

```
bit 6 (0x40) 赤ドラ / bit 5 (0x20) ドラ / bit 4-0 (0x1F) 牌種 0〜28
有効な値: 0-28（通常） / 32-60（ドラ） / 64-92（赤ドラ）
存在しない値: 29-31, 61-63, 93-115
```

クライアントは範囲外IDを受け取ると西（28）にフォールバックして描画するため、
**交換した牌が意図しない絵柄になる。**

→ `reserved_tiles` を有効なID空間から作るように直してほしい。

---

## A-2. フリテンでも `agari_pending` が飛んでくる

`on_agari_pending` はフリテン判定をせずに送っている。
一方 `resolve_pending_agari` は受け取った時点でフリテンを弾く。

結果、プレイヤーには**ロンボタンが出るが、押しても何も起きない。**
拒否された理由も伝わらないので「反応しない」ようにしか見えない。

判定はサーバーに一任する方針なので、クライアント側でフリテンを判定して
出し分けることはしない（一度実装したが方針に反するため撤回した）。

**対応案はどちらでも良い:**

1. フリテンなら `agari_pending` を送らない
2. `agari_pending` に `"can_win": false` のようなフラグを含める

**2 を希望。** フラグがあれば「フリテンのためロンできません」と表示でき、
プレイヤーに理由を伝えられる。1 だと理由を出せない。

---

## A-3. `agari_accepted` に拒否理由が無い

現在の内容:

```json
{"type": "agari_accepted", "data": {"accepted": true, "is_win": false, "liquidation": null}}
```

`is_win: false` になった理由が分からない。フリテンなのか、手が足りないのか、
別の理由なのかを区別できないため、プレイヤーに何も説明できない。

→ `"reason": "furiten"` のような文字列を足してほしい。

---

## A-4. 流局までの打牌数を 16 → 17 にしてほしい【仕様変更】

現在の流局条件:

```python
if all(len(player.discards) >= 16 for player in self.state.players):
```

**`game_engine.py` の 560 / 597 / 622 行の3箇所に同じ条件がある。**
（通常の流局・フリテンで和了拒否後の流局・和了見送り後の流局）
1箇所だけ直すと局面によって手数が変わってしまうので、3箇所すべて要変更。

### 現状と要望

```
山牌 34枚 − 手牌 13枚 = 打てる牌 21枚

現在:  16打で流局  →  21 − 16 = 5枚 余る
要望:  17打で流局  →  21 − 17 = 4枚 余る
```

### 理由

**設計意図が「17打」であり、チュートリアルも17手で作られている。**
`TutorialScenario.cs:478` に「17手ぶん。うち最初の15手は自動で流し、残り2手を
プレイヤーに打たせる」とあり、チュートリアルはサーバーに接続せず
クライアント側だけで完結するため、サーバーの16打とは独立に組まれている。

このままだと**チュートリアルで17手を覚えたプレイヤーが、本編では16手になる**という
食い違いが残る。本編を17打に合わせたい。

---

## A-5. 獲得点30000で勝ち、という勝利条件を追加してほしい【仕様変更】

血（HP）を削り切る以外に、**獲得点の累計が 30000 に達したら勝ち**という条件を足したい。
既存のHP0による決着は**そのまま残す**。どちらか早く成立したほうで対局が終わる。

### 判定のタイミング

**局の終わり（liquidation を出す時点）** で判定してほしい。
局の途中で 30000 を超えても、その局が終わるまでは続行。

### 累計の中身

`liquidation.winner_gain` を各プレイヤーについて足し込んだ値。
つまり「その対局でこれまでに勝ち取った血の総量」。

### 送ってほしい情報

現状 `liquidation` は**その局ぶんの増減**しか入っていないため、
クライアントは自前で足し込むしかない。途中で再接続したり、
演出中にメッセージを取りこぼしたりすると累計がずれる。

`liquidation` か `round_end` に**累計そのもの**を入れてほしい。

```json
"liquidation": {
  "winner_gain": 4800,
  "winner_total_gain": 18600,   ← 追加してほしい（勝者の累計）
  "loser_total_gain": 7200,     ← 追加してほしい（敗者の累計）
  "target_gain": 30000          ← 追加してほしい（勝利に必要な値）
}
```

`target_gain` も送ってもらえると、サーバー側で値を変えたときに
クライアントの表示が勝手に追従できる（B-3 と同じ理由）。

### 決着メッセージ

30000到達で終わった場合も `game_end` で終わると思っているが、
**HP0で終わったのか獲得点で終わったのか**を区別したい。
`game_end` に決着理由（例 `"reason": "hp_zero" | "target_gain"`）を入れてほしい。

### クライアント側の現状

**表示だけ先に作ってある**（`ScoreGaugeUI`）。
画面の左に相手、右に自分のゲージを出し、`winner_gain` を自前で足し込んで
`累計 / 30000` の比率で伸ばしている。0のときもゲージ自体は出している。

**勝敗の判定はクライアントでは一切していない。**
サーバーが上記に対応したら、累計は自前計算をやめてサーバーの値に差し替える。
それまではクライアント側の表示だけが先行している状態になる。

---

## A-6. 配牌を「両者の準備完了」より前に始めてほしい【体感速度】

局が変わるたびに **10〜35秒** 待たされる。実測（クライアント側のログ）:

| | `phase=dealing` → `dealing_completed` |
| :--- | :--- |
| 1回目 | 21.2 秒 |
| 2回目 | 34.6 秒 |
| 3回目 | 12.4 秒 |

### 原因

`TileWall.deal()` が**当たりが出るまで引き直す方式**になっている。

```python
# tile_wall.py
while True:
    dealt_tiles = self.tiles[:count]
    rest_tiles = self.tiles[count:]
    # 配った 34 枚から満貫以上の聴牌形を探索
    hands = HandAnalyzer.search_tenpai(dealt_tiles, rest_tiles, self.dora_id)
    if hands:
        self.tiles = rest_tiles
        return dealt_tiles, random.choice(hands)
    # 見つからなければ同じ牌山を再シャッフルして再探索
    self.shuffle()
```

34枚から満貫以上の聴牌形が出るまで、シャッフルと `search_tenpai` を繰り返す。
何回転するかは運次第なので、待ち時間が毎回大きくぶれる。
これを `_deal_tiles()` が**プレイヤー人数ぶん直列**で回している。

### いつ走っているか

```
confirm_next_round()        ← 両者が「準備完了」を押し終える
  └ 全員そろったら
      _prepare_next_round()
      _start_round()
        └ _set_phase(DEALING)
        └ _deal_tiles()     ← ここで 10〜35 秒
```

**両者の準備完了を待ってから配牌計算に入っている。**
プレイヤーから見ると「準備完了を押してから待たされる」形になる。

### 要望：打牌フェイズ中に次局の配牌を仕込んでおいてほしい

**打牌フェイズが始まった時点で、次局ぶんの配牌をバックグラウンドで計算し始めてほしい。**
`_deal_tiles()` は出来上がりを受け取るだけにする。

打牌フェイズは17打ぶんあり手番のタイムアウトも無いので、
10〜35秒の計算を隠すのに十分な時間がある。準備完了まで待つ案よりも余裕がある。

```
[打牌フェイズ開始]  ← ここで次局ぶんの計算を投げる（別スレッド）
   ... 17打 ...     ← この裏で計算が終わる
[局終了]
[両者 準備完了]
[次局開始] _deal_tiles() → 出来上がりを受け取るだけ（待ち時間ゼロ）
```

### 前倒しできる根拠

配牌は**直前の局の結果に一切依存していない**。
`TileWall.reset()` が牌山を作り直し、ドラも `_initialize_wall()` の中で引き直している。
局の点数・HP・捨て牌などは配牌に入ってこない。

`self.tile_wall` を触っているのは **`_deal_tiles()` の中だけ**（`game_engine.py` 84〜118行）。
局の途中では誰も参照していないので、裏で作っておいても現在の局には影響しない。

### 実装するときに気をつけてほしいところ

- **`self.tile_wall` を裏で reset しないこと。** 別に `TileWall()` を1つ作って、
  そちらで計算してから丸ごと差し替えるのが安全（現在の局と道具を共有しない）
- **`random` を別インスタンスにしてほしい。** ワーカー側は `random.Random()` を
  自前で持つ形にしないと、本体側の乱数（スキルなど）と食い合う
- **`reserved_tiles`（マリガン用）も一緒に作っておける。** 配られた牌から導いているだけなので、
  ワーカー側で計算して持っておけば `_deal_tiles()` が軽くなる
- **仕込みが間に合わなかったときは今までどおり計算する**フォールバックを残してほしい。
  打牌が異常に速く終わった場合に備えて
- **対局が終わったら捨ててよい**（最大局数到達・HP0）。無駄になるだけで害はない

### 初局について

対局開始（1局目）の配牌も同じ経路なので同じだけ待つ。
**マッチングが成立した時点で仕込み始めてもらえる**と、
「対戦相手を待っています」の裏に隠れて、こちらも短くなる。

### 参考：他に考えた案（採用しなくてよい）

- 局が終わった時点で仕込む → 打牌中に比べて隠せる時間が短い
- 配牌をプールしておく → 効果は大きいが持ち回りが増える

### あわせて：計算がイベントループを止めている

`_deal_tiles()` は同期関数なので、回している間 **asyncio のイベントループごと止まる**。
同時に別の対局が走っていると、そちらの通信もまとめて止まるはず。

`asyncio.to_thread()` などで別スレッドへ逃がしてもらえると、
待ち時間そのものは変わらなくても**他の対局への巻き込みが無くなる**。

### 初局について

対局開始（1局目）の配牌も同じ経路なので同じだけ待つ。
**マッチングが成立した時点で仕込み始めてもらえる**と、
「対戦相手を待っています」の裏に隠れて、こちらも短くなる。

### クライアント側

**この待ち時間はクライアントでは短縮できない。** サーバーの計算が終わるまで
`dealing_completed` が来ないため。こちらは待っているだけの状態。

---

## B-1. 相手の手牌と待ちが全員に送られている【要検討】

`hand_selection_completed`（`on_selected`）が、**両プレイヤーぶんの
`hand` / `waits` / `wall` を全員にブロードキャスト**している。

```python
payload = [{ "client_id": ..., "hand": [...], "waits": player.waits, "wall": player.wall }
           for i, player in enumerate(players)]
await self._broadcast_match_members(match_id, {"type": "hand_selection_completed", "data": {"hands": payload}})
```

つまり**相手の手牌と待ち牌がクライアントに届いている。**
公式クライアントは表示していないが、通信を覗けば相手の待ちが分かる状態。

対人戦としては不利になり得るので、扱いを相談したい。

- 流局時の公開に必要なら、**そのタイミングで送る**方が安全
- 透視スキルも本来はサーバーが「見せてよい範囲」を決めるべき

すぐ困っているわけではないので優先度は B。ただ**仕様として意図しているか
だけは確認したい。**

---

## B-2. 局番号を各メッセージに入れてほしい

`round_start` には `round` が入っているが、それ以外のメッセージには無い。

クライアントは現在、**自前で局数を数えている**（和了・流局のたびに +1）。
そのため取りこぼすとサーバーとズレる。実際に「局の開始」と「対局の開始」を
取り違えて第1局に巻き戻る不具合が起きていた（クライアント側で修正済み）。

→ `round_end` / `phase_change` あたりに `round` を入れてもらえると、
サーバーを正として同期でき、この種のズレを構造的に防げる。

---

## B-3. 打牌数（残り手数）を送ってほしい

流局は「両者16打」で判定されているが、クライアントはこの値を知らない。

現在「相手の待ち候補を絞り込む」表示を実装しており、
**あと何手で流局か**が分かると、終盤の緊張感を演出に反映できる。

→ `discard_completed` に `"discard_count": n` と `"max_discards": 16` を
入れてもらえると助かる。定数がサーバー側で変わっても追従できる。

---

## C-1. `debug_add_hp` の扱い

クライアントに `H` キーでHPを1000増やすデバッグ機能が残っていた（無効化済み）。

```json
{"type":"action","action":"debug_add_hp","data":{"amount":1000}}
```

サーバーの `action_handlers` には登録が無いので現状は弾かれるはずだが、
**もし実装されているなら本番では無効化してほしい。**

---

## 補足：クライアント側で対処済みのもの（対応不要）

参考まで。これらはサーバーの変更なしで直した。

- `agari_pending` の牌IDのキーが `tile` なのにクライアントが `tile_id` を見ていた
  （常に 0 = 一萬になっていた）→ クライアント側を `tile` に合わせた
- 局の開始と対局の開始の取り違え → クライアント側で `round_start` の扱いを修正
- 演出中に届いたメッセージの取りこぼし → クライアント側で保留キューを実装
