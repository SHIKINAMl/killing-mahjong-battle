# チュートリアル台本 設計メモ（2026-08-05）

BACKLOG 要望7「セリフで危機を煽る」で入れ替えたセリフの設計書。

**セリフの本体はここに置かない。** `TutorialScenario.BuildDefault()` が唯一の原本で、
`Assets/Resources/Tutorial/TutorialScenario.asset` はそれを写したもの。
2か所に同じ文章を置くと必ず片方が古くなるので、ここには「なぜそう書いたか」だけを残す。

---

## 適用先について（重要）

**実際に使われているのはアセットのほう。** `OpeningScene` の `TutorialManager` が
`TutorialScenario.asset` を参照しているため、`BuildDefault()` は呼ばれない。

`BuildDefault()` を編集したら、必ずアセットを作り直すこと:

```csharp
var path = "Assets/Resources/Tutorial/TutorialScenario.asset";
var asset = AssetDatabase.LoadAssetAtPath<TutorialScenario>(path);
var src = TutorialScenario.BuildDefault();
var keepName = asset.name;                 // CopySerialized は名前まで複製する
EditorUtility.CopySerialized(src, asset);
asset.name = keepName;
EditorUtility.SetDirty(asset);
AssetDatabase.SaveAssets();
UnityEngine.Object.DestroyImmediate(src);
```

---

## 方針

### 全部、女の子に喋らせる

**地の文は使わない。** `TutorialSpeaker.System` は仕組みとしては残っているが、
台本では1行も使っていない（`speaker` はすべて `Enemy`）。

はじめは詩的な部分を地の文に逃がしていたが、彼女に言わせたほうが強かった。
たとえば冒頭の対比は、地の文だと状況説明にしかならないが、
彼女の口から出ると**プレイヤーへの詰問**になる。

| 地の文だったときの案 | 彼女に言わせた形 |
| :--- | :--- |
| ──この血は "払った" のか。 | ねえ、その血。あなたは "払った" つもり？ |
| 彼女は、嘘をつくときに目を伏せなかった。 | ……ほら。ちゃんと目を見て言ったでしょう？ |
| 彼女の体力ゲージが、こちらより短い。 | ……ねえ、気づいてる？ 私のゲージ、あなたより短いのよ。 |

3つめのように、**地の文だと「主人公の観察」でしかないものが、
彼女に言わせると「相手からの告白」になる**。情報量は同じで、意味が変わる。

口調は既存キャラのまま（「〜わ」「〜のよ」）。説明は短く、断定を強くしている。

### 一本の対比を全体に通す

冒頭で「この血は "払った" のか、"抜かれた" のか」を立て、最期に彼女自身が反転させて閉じる。
局ごとの煽りは、すべてこの一本に紐づける。

| 局 | 対比のどこにいるか |
| :--- | :--- |
| 第1局 | 「自分で決めて払った」と思わせる |
| 第2局 | 払ったのに何も起きない。**戻らないことだけが確定する** |
| 第3局 | 嘘によって「決めた」が崩れる。**決めさせられていた**と明かす |
| 第4局 | 彼女も払っている。**加害者も同じ穴の中にいる**と示す |
| 第5局 | 倒れながら彼女が問い返す。**決める側が入れ替わる** |

### ルールを、説明ではなく脅しに変える

新しい情報は足していない。既にあるルールの冷たい面を言葉にしただけ。

- 第1局：満貫は1倍＝**勝っても払った分が戻るだけ**。「あなたは1滴も増えていないの」
- 第2局：流局は助かったのではなく**支払いの先送り**。「延ばした分だけ、次が重くなる」
- 第4局：彼女は能力に12700払った直後で血が薄い。彼女自身にゲージを指させ、
  **第5局の決着の伏線**にした

### 小道具は画面にあるものだけ

卓の上の**契約書とペン**は実際に描かれている（`OpeningScene` の背景）。
無いものを書くと嘘になるので、触れるのはこれと、賭け金の数字・体力ゲージ・牌だけ。

---

## 参考にした作法（『Inverted Angel』）

`d:\myData\scenarios\inverted_angel\` のシナリオから取った技法と、その採否。

| 技法 | 採否 | 備考 |
| :--- | :--- | :--- |
| 詩的・哲学的な対比モノローグ | **採用** | 「払った／抜かれた」に各3例。冒頭と最期で対にする |
| 信用できない語り手 | 部分的 | 第3局の嘘（待ち牌は東）がこれにあたる |
| 日常と異常が同居する小道具 | **採用** | 契約書＝日常の書類、インク＝血 |
| ト書きの一体化 | **見送り** | 下記 |
| ノード登録（推理UI） | **不採用** | 下記 |

### ト書きを見送った理由

`TutorialLine` は `speaker` と `text` しか持たないので、現状は書けない。
やるなら `cue` を1つ足すのが最小の変更で、既存の演出に割り当てられる。

| ト書き | 現状 | 割り当て先 |
| :--- | :--- | :--- |
| 心音 | **ある** | `HeartbeatEffect`（瀕死時の赤ビネット。sortingOrder 91） |
| 血 | **ある** | `PixelBloodEffect` / `BloodTransferEffect`（どちらも画面未確認） |
| 暗転 / 明転 | **ある** | `PhaseTransitionUI` |
| ノイズ | 無い | 新規。画面を数フレーム揺らす程度で足りる |

### 推理ノードを入れない理由

参考元のノードは「プレイヤーが仮説を選ぶ」ための器で、こちらは仮説を選ぶゲームではない。
形だけ真似ると、押しても何も起きないカードが増えるだけになる。

---

## 触るときの注意

- **数値は一行も変えない。** `betAmount` / `yakuList` / `formulaText` / `rankText` /
  `enemyWinningHan` / `isTankiWin` / `drawDamageToPlayer` は触らない。
  `TutorialScenario.cs` 冒頭の収支表がそのまま成立しなくなる
- `betPromptText` は `{0}` を、`inheritedBetLines` は `{0}` `{1}` を必ず残す
- 変更後は `TutorialScenario.Validate()` が true であることを確認する
- 第1局は `revealBoardAfterLineIndex = -1`（イントロを全部流してから盤面）。
  現在14行あるので、盤面が出るまでが長い。テンポが気になるなら
  `revealBoardAfterLineIndex = 3` にして盤面を先に出す

## 現状（2026-08-05 時点）

- セリフ総数 **121行**（改稿前は75行）。うち地の文 **0行**
- 最長のセリフは39文字。改稿前の最長40文字より短いので吹き出しの表示は変わらない
- `Validate()` = true
