# 引き継ぎメモ 2026-07-30

このセッションで行った変更のまとめ。次回はこのファイルを先に読むこと。

- 大半は commit `15e59f8「チュートリアル第４局まで」` に含まれる
- 未コミットは `Assets/Scripts/Managers/TutorialManager.cs` のカットイン修正のみ（後述 §13）
- Unity 6000.2.12f1 / 対象シーン `Assets/Scenes/OpeningScene.unity`

---

## 検証状態について（重要）

**このセッションの変更はすべて「コンパイル0エラー」と「データ・座標の数値検証」までしか行っていない。**
Play モードでの動作確認は一度もしていない。以下はすべて実機確認が必要。

検証は MCP for Unity（`http://127.0.0.1:8080/mcp`）経由で行った。
MCPツールがClaude側に載らない場合は HTTP を直接叩く。`execute_code` で Unity 内の
リフレクション・座標計算を実行して確認するのが有効だった。

---

## 1. sortingOrder の全面再編

`Assets/Scripts/Common/UISortingOrders.cs`

**最大値 32767 → 99 に圧縮。** 10 の位で用途を区切る体系にした。

| 帯 | 用途 |
|---|---|
| 0–9 | 盤面（山牌・背景パーティクル） |
| 10–19 | 常設UI（ドラ・情報パネル通常） |
| 20–29 | 情報パネル強調・ツールチップ |
| 30–39 | システムオーバーレイ |
| 40–49 | 空き |
| 50–59 | ロン待機・結果パネル・軽い演出 |
| 60–69 | チュートリアル誘導 |
| 70–79 | 牌アニメ・実況 |
| 80–89 | ダイアログ・最前面パネル |
| 90–99 | 全画面演出・システム最前面 |

- 直値だった 15/16/19/20/1/2/5/32767 を定数化（`WallBase` `InfoPanelNormal` `MouseCursor` 等）
- `AgariSelectionMax` → `AgariSelection` に改名
- シーン側も追随：`役ListPanel`/`勝敗Canvas` 100→55、`HeartbeatEffect` 31000→91、`MatchMomentumUI` 31500→92

**存在しない Sorting Layer `"UI"` への代入を8箇所削除。**
`ProjectSettings/TagManager.asset` の Sorting Layer は `Default` のみ。
未知のレイヤー名は Unity に無視されるので、これらは全て no-op だった。
重なり順は必ず `sortingOrder` で調整すること。

**意図的に順序を確定させた3組**（元は同値でヒエラルキー順の運任せ）:
`CommentaryFlow > TileAnimationLayer` / `YakuSelection > WaitDisplayFront` / `MouseCursor > AgariSelection`

---

## 2. 牌のクリック判定（触り心地）

`Assets/Scripts/UI/TileInteraction.cs`

牌は幅45に対し間隔25で並ぶため、隣と20px（幅の44%）重なる。
`SetSiblingIndex(i)` で右隣が手前に描かれるので、
**1牌あたり45pxのうち25pxしか自分の判定がなく、残り20pxは隣が拾っていた。**
→「乗っているのに反応しない」「隣の牌を打牌してしまう」の原因。

`ICanvasRaycastFilter` を実装し、手前に描かれる牌に覆われている点を判定から除外。
レイアウトの実座標から毎回計算するので、手牌・山牌・2段組みでも自動追従する。

判定境界が見た目の境界（x=25）と一致することを実測済み。
牌からはみ出した子要素（`透視マーク` 等、`raycastTarget=true` だった）の上も拾わなくなった。

**まだ押しづらい場合**は判定のズレではなく「見えている幅が25pxしかない」ため。
`HandUI` の `Tile Spacing X` を 30〜35 に広げると押せる幅が広がる。

---

## 3. 瀕死ビネット（HeartbeatEffect）

`Assets/Scripts/UI/Effects/HeartbeatEffect.cs`

明滅（1.2秒周期でα0↔0.5往復）をやめ、**じんわり出して維持**する方式に変更。

| 状態 | 赤ビネットの濃さ |
|---|---|
| HP30%以下 | 0.28 で維持 |
| HP10%以下 | 0.45 で維持（黒ビネットを0.6倍で重ねる） |
| 回復・死亡・決着 | 1.5秒かけてフェードアウト |

- インスペクタ調整可：`Warning Alpha` / `Danger Alpha` / `Black Vignette Ratio` / `Fade Duration`
- 鼓動音は残した（通常1.2秒 / ピンチ0.6秒間隔）
- `Awake` で `raycastTarget=false` を強制（全画面Imageが打牌のタッチを奪うのを防ぐ）
- `UpdateHeartbeat` に早期リターン追加。`SetHP` はロン演出中に毎フレーム呼ばれるため

決着時にビネットが結果画面に被る問題も対応：
`GameUIManager.ShowGameResultRoutine()` 冒頭で `PlayerInfoUI.StopHeartbeatEffect()` を呼ぶ。
`heartbeatSuppressed` フラグで再開を抑止（解除は `SetMaxHP`）。

---

## 4. 戦況グラフ（MatchMomentumUI）

`Assets/Scripts/UI/Effects/MatchMomentumUI.cs`

`Awake` 末尾の `gameObject.SetActive(false)` を `Start` へ移動。
Awake で無効化すると、非アクティブ配置時に `ShowMomentum` の `SetActive(true)` が
Awake を誘発して自分を無効化し直し、直後の `StartCoroutine` が失敗する。

- `CanvasGroup` の `blocksRaycasts`/`interactable` を false に
- `DrawGraph` で `Canvas.ForceUpdateCanvases()` を挟み、サイズ0なら警告して抜ける

namespace が `KillingMahjongBattle.UI.Effects` のまま（他62ファイルは `KillingMahjong.*`）。**未対応。**

---

## 5. 打牌ピッチ

`Assets/Scripts/Managers/AudioManager.cs`

連続打牌のピッチ上昇を半音→四分音に。1オクターブ到達が12打→**24打**。
インスペクタの `Discard Pitch Step Semitones`（0.1〜2.0）で調整可。1.0 で元の半音刻み。

---

## 6. オープニングのまぶた（BlinkEffectUI）

`Assets/Scripts/UI/BlinkEffectUI.cs`

3つの原因が重なって演出が丸ごと見えていなかった。

1. **まぶたパネルが非アクティブのまま保存されていた**（ルーチンは最後に無効化するが開始時に有効化していなかった）
2. **pivot が (0.5, 0.5)** → スケールを下げるとパネル自身の中心に縮み、まぶたに見えない
3. **親Canvas の sortingOrder が 0** → 卓や紙より奥

修正：`Awake` で閉じた状態にし、pivot/アンカーをコードで矯正（上まぶた上半分・下まぶた下半分）、
`sortingOrder = UISortingOrders.OpeningEyelid (98)`、開始時に `SetActive(true)`。
`initialWait` / `finalOpenDuration` をインスペクタ化。

---

## 7. チュートリアル：賭け金の持ち越し

`TutorialManager` に場（`_pot`）の概念を追加。

- 流局では賭け金が決着せず場に残る
- **流局の次の局は前局と同額が自動で賭けられる**（賭け金フェイズの操作・指示をスキップ）
- 次のロンで、場の分も打点と一緒に血として移動する

`TutorialRoundData.inheritedBetLines`（`{0}`=持ち越し額 `{1}`=場の総額）で説明セリフを差し込む。
ロン演出の表示額も持ち越し込みの総額（`displayScore`）に変更。表示とHP増減が食い違わないように。

第3局にあった `drawDamageToPlayer = 1000` の二重取りを削除（第2局で既に引かれていた）。

**判断メモ**：流局そのもののダメージ（`drawDamageToPlayer`）は賭け金とは別枠のペナルティとして残した。
「流局ダメージ＝賭け金を失うこと」が正しければ第2局・第4局の値を0にする。

---

## 8. チュートリアル：ロンで血を得る

`RunPlayerRon` がプレイヤーHPを一切動かしていなかった。
相手から減った分がそのまま自分の血になるよう修正（相手の残り血を超える打点でも実際に奪えた分だけ）。

**判断メモ**：対称に `RunEnemyRon` でも相手が血を得るようにした。
これは「ロンしたら血を得る」を相手側にも当てはめた判断。据え置きが正しければ戻す。

---

## 9. チュートリアル：能力（手順⑱⑲⑳）

### 進行順（第4局）

```
イントロ
  ↓
能力の宣言 → 透視・牌交換・役強化を順に実演 → 能力の説明 → 強化の説明 → 役一覧へ誘導
  ↓
『自動』ボタンで手牌を決定        ← 能力の説明はこの前に来る
  ↓
賭け金 → 打牌 → 敵が2打目で放銃 → ロン
```

能力は手牌フェイズ専用（`AbilityUI.OnActivateClicked` が `HandSelection` 以外を弾く）なので、
実演もこのフェイズ内に置いた。さらに手牌を決める前に済ませ、説明を聞いてから組める流れにした。

### 敵が実際にスキルを発動する

`RunEnemySkillActivation` が本編の `GameUISkillController` と同じ手順を踏む。

1. スキルSE（`AudioManager.PlaySkillSE`）
2. カットイン（`PhaseTransitionUI.PlaySkillCutinAnimationRoutine`、`isLocalPlayer:false`）
3. **コスト（血）の支払い** — `GameRules.GetSkillCost` の実コストを敵HPから引く
   （透視1500 / 牌交換1200 / 役強化10000 = 計12700）
4. 能力ごとの効果

効果の実装：
- **透視** → `ApplyPerspectiveMarks(3)`。`TileVisual.SetExposed(true)` でプレハブの `透視マーク` を3枚に表示。
  手牌がまだ無い段階なので山牌から散らして選ぶ。局が変わるとき `ClearPerspectiveMarks()` で消す
- **役強化** → `BoardStateManager.EnemyBoostHandBonus["清一色"]=1` を実際に積み、
  `YakuListUI.UpdateBoostData` で反映。直後の役一覧誘導で確認できる
- **牌交換** → 本編でも敵使用時はプレイヤー側に見える効果がないため、カットイン+コストのみ

`AbilityUI.IsDisplayOnly` を追加。実演中は押しても反応しない（押せると `DialogueUI` が
チュートリアルのセリフを上書きして進行が止まる）。

---

## 10. チュートリアル：第4局の配牌差し替え

第4局を流局→**自分ロン**に変更。敵が2打目で放銃する。

```
手牌(13枚) : 111p 444p 777p 99p 東東
待ち       : 9p / 東（シャンポン）
和了牌     : 9p（敵の2打目）
和了形     : 111p 444p 777p 999p 東東
役         : 対々和(2飜) + 混一色(門前3飜) = 5飜 = 満貫 8000
```

**跳満12000にすると敵の血が尽きて第5局が成立しない**ため満貫に抑えている。
制約『満貫手以下での開始は不可』があるので、これより安くもできない。

第4局専用の山牌 `AbilityWall()` を新設（手牌13枚 + 萬子1-9 + 索子1-9 + 西 + 2p + 5p = 34枚）。
**残り21枚に待ち牌 9p/東 を1枚も含めない**こと（プレイヤーが自分の待ちを打てるとフリテンの説明が必要になる）。

聴牌チェックの応答も第4局だけ `manganHandYaku = {対々和, 混一色}` / `manganHandHan = 5` に上書き。

### 血の流れ（実データで計算済み）

```
開始                                          自分=20000  相手=20000
第1局  自分ロン  跳満12000 + 場1000 = 13000    自分=33000  相手= 7000
第2局  流局     ダメージ1000 / 場1000は持越   自分=32000  相手= 7000
第3局  相手ロン  役満16000 + 場2000 = 18000    自分=14000  相手=25000
第4局  能力コスト -12700                      自分=14000  相手=12300
       自分ロン  満貫 8000 + 場1000 =  9000    自分=23000  相手= 3300
第5局  自分ロン  役満32000 + 場1000 = 33000    自分=26300  相手=    0  決着
```

`BuildDefault()` 先頭のコメントにこの表を書いてある。**数値を触ったら必ず最後まで通して確認すること。**

---

## 11. 自力で満貫手を組めたら即決定

サーバー非接続で手牌を評価できないため、「台本の満貫手と同じ13枚がそろっているか」で判定する。

この判定が「満貫以上」と等価になる根拠（第1局・第5局で実測）:
- 山の萬子は台本の13枚しかない → 同じ色で13枚組む方法は1通り
- 他の色は9枚しかない → 清一色を作れない

13枚そろった時点で判定し、成立していれば `自動` を出さず `決定` だけ出す（`HandButtonStage.DecideOnly`）。
`rejectFirstConfirm` もスキップされる。判定は13枚そろった瞬間の1回だけ。

---

## 12. チュートリアル誘導（矢印・マスク）の座標バグ

`Assets/Scripts/Common/UIRectUtility.cs`（新規）

`GetWorldCorners` の返す値は Canvas の RenderMode で意味が変わる。
Overlay ならスクリーンのピクセル座標、ScreenSpaceCamera ならカメラ基準のワールド座標。
前者のつもりで後者を `InverseTransformPoint` に渡すと矩形が画面隅に潰れる。

**`役Canvas` だけが ScreenSpaceCamera** なので、そこだけ誘導が壊れていた（実測 (-399,-295) 1x1）。
`TutorialMaskUI` / `TutorialArrowUI` をスクリーン座標経由に変更 → (260,200) 280x280 = 右上。

> 注意：`背景Canvas`・`マージャン卓`・`役Canvas` は ScreenSpaceCamera で、
> **エディタ上では常にスケール0に見える**。これは正常。壊れていると誤診しないこと。

矢印は `Awake` で自分自身にも Canvas を足しているため、
**変換先の Canvas は必ず親から探すこと**（`transform.parent.GetComponentInParent<Canvas>()`）。
`anchoredPosition` は親の中の座標なので、変換先は親の RectTransform。

`GuideTo(target, useMask: false)` を追加。マスクは穴の外側のクリックを全て食べるので、
**セリフ送りと併用する場面では必ず `useMask: false` か、`PlayLines` の前に `ClearGuide()` すること。**
（`TutorialMask` は order 60、`DialogueCanvas` は 16。出したままだと送りボタンが押せず進行が止まる）

誘導先は `YakuListUI.GuideTargetRect`（画面右上の「役一覧」画像そのもの）を使う。

---

## 13. 差分リビルドのキャッシュ／カットインの停止

### `GameUIVisualController.InvalidateRebuildCache()`（追加）

`RebuildAllTilesFromState` は前回と牌IDの構成が同じなら再生成を省く。
だが `ClearAllTiles()` は牌をプールへ返すので、その判断が成立しない。

**プレイヤーが台本と同じ満貫手を自分で選んで `自動` を押すと**、
押下前後で山21枚・手牌13枚が完全に一致 → 差分0件 → 「変化なし」と誤判定 → **盤面が空になる**。

`ClearAllTiles()` から `InvalidateRebuildCache()` を呼ぶよう修正。本編の局またぎにも効く。

### PhaseTransitionUI の一時有効化（★未コミット）

`TutorialManager.SetupBoard` はフェーズ演出を出さないよう `PhaseTransitionUI` を無効化している。
そこへスキルのカットインを流すと、内部の `StartCoroutine` が
`Coroutine couldn't be started because the game object is inactive` で失敗し進行が止まる。

カットインの直前だけ有効化し、終わったら元に戻すよう修正。
`Start()` で Canvas の `sortingOrder` を設定しているので、有効化後に `yield return null` を挟むこと。

> **同種の罠**：非アクティブなオブジェクトを `SetActive(true)` した直後は `Start()` が未実行。
> `AbilityUI`（`realAbilities` が null で NRE）でも同じ事故が起きた。
> 有効化した同フレームに使わず、1フレーム待つか遅延初期化にすること。

---

## 未対応・未確認

### 実機確認が必要
Play モードでの通しプレイは**まだしていない**。特に第4局を通しで流すこと。
（UIの生成・表示と数値だけは Play モードで確認済み。§15 参照）

### 既知の未対応
- `Assets/_Recovery/0 (8).unity` `0 (9).unity` は sortingOrder 未整列（バックアップと判断して除外）
- `UISortingOrders.MulliganPromptText` / `SkillDimmer` は元から未使用の定数
- `TutorialManager.IsAbilityUsableByPlayer` はどこからも参照されていない死んだプロパティ

---

# 追記（同日・2回目のセッション）

## 14. 獲得金・失う金を「賭け金 × 役の倍率」に変更（最新は §21 の追記も見ること）

**賭け金は確定した時点で両者の血から引かれる**（§21 で変更）。
それとは別に、決着時の増減は式で決まる（`GameRules`）:

```
勝者が得る額 = 勝者自身の賭け金 × 勝者の役の倍率
敗者が失う額 = 敗者自身の賭け金 × 勝者の役の倍率（勝者が単騎で上がったら2倍）
倍率: 満貫1 / 跳満1.5 / 倍満2 / 三倍満3 / 役満4 / ダブル役満(26飜)8
```

得と損は別計算なので額は一致しない（血の総量は保存されない）。
場の表示（`BetPotUI`）は積まれている賭け金の**情報表示**で、表示額が移動するわけではない。

- `GameRules.CalculateWinnerGain(bet, han)` / `CalculateLoserLoss(bet, han, tanki)` を追加。
  倍率表は元からあった `GameRules.GetMultiplier(han)` をそのまま使っている
- `TutorialManager`：`PlaceBet()` は血を動かさず賭け金を積むだけ。`ClearStakes()` で精算
- `TutorialRoundData`：`score`（生の金額）を削除し、`enemyWinningHan` と `isTankiWin` を追加。
  PlayerRon の飜数は `manganHandHan` を使う（プレイヤーの手がそのまま勝者の役）
- 流局では血が動かない。賭け金は次の局へ積み増される（`drawDamageToPlayer` は 0 のまま）
- `BettingUI` の「予想報酬」も `CalculateWinnerGain` を使うよう統一。
  **元は `Mathf.FloorToInt(bet * 倍率)` で表示だけこの式、精算は生の金額（score）を使っていた。
  これが「もらえる点の計算がおかしい」の正体。**

血の流れ（Python と、実際の `BuildDefault()` データ＋`GameRules` の両方で通し検証済み）:

```
開始                                                      P20000 / E20000
第1局 賭け各2000  自分ロン 清一色6飜=1.5倍
                    自分+3000 / 相手-3000                 P23000 / E17000
第2局 賭け各1000  流局（血は動かない・賭け金は持ち越し）  P23000 / E17000
第3局 自動1000で賭け金は各2000
      相手ロン 四暗刻単騎13飜=4倍・単騎2倍
                    相手+8000 / 自分-16000                P 7000 / E25000
第4局 能力コスト-12700                                    P 7000 / E12300
      賭け各2000  自分ロン 対々和+混一色5飜=1倍
                    自分+2000 / 相手-2000                 P 9000 / E10300
第5局 賭け各2000  自分ロン 純正九蓮宝燈26飜=8倍
                    自分+16000 / 相手-16000               P25000 / E    0（決着）
```

数値を触るときの制約:
- 第3局で自分が死なないこと（単騎の2倍が効くので損失が跳ね上がる）
- 第4局の前に相手が能力コスト12700を払えること／払った後も生き残ること
- 第5局で相手が死ぬこと（賭け金×倍率 ≧ 相手の残り血）
- 全局とも満貫以上（制約『満貫手以下での開始は不可』）

**能力コストの支払いは賭け金より前**（手牌フェイズ）。§10 の表は順序が逆だったので直した。

### ⚠ 本編（サーバー対戦）との食い違い

`Network/Handlers/BettingMessageHandler.cs` は `bet_completed` を受けて
**賭け金を両者のHPから引いている**（サーバーがそう送ってくる前提の実装）。
新ルールでは賭け金で血は減らないので、**本編を新ルールに合わせるにはサーバー（Python）側の対応が必要**。
Python は担当外なので触っていない。この式で動いているのは現状チュートリアルだけ。

## 15. 場の血の表示（BetPotUI）

`Assets/Scripts/UI/BetPotUI.cs`（新規）＋シーンに `Canvas/場の血Canvas`（order 14 = `UISortingOrders.BetPot`）。
`GameUIManager.betPotUI` に配線済み。**シーンは保存した。**

**配置済みのシーン（両方）:**
- `Assets/Scenes/OpeningScene.unity`（チュートリアル）
- `Assets/Scenes/UIテストシーン.unity`（**本番のマルチ対戦**）

両シーンはUIの構成・座標がまったく同じなので、同じ位置・同じ設定で作れる。
シーン側の変更はこの1件だけで、他（獲得金ルール・軽量化・カーソル・カットインの項目追加）は
すべてスクリプト側なので両シーンに自動で効く。新規 SerializeField はコード側の既定値が入る
（`UIテストシーン` でも splatter 500〜800 / 影オフ / カーソル -12度 が入っていることを確認済み）。

> 注意: `UIテストシーン` には `AudioManager` が置かれていない（`OpeningScene` にはある）。
> シングルトンなので他シーンから遷移すれば持ち越されるが、**このシーンを単体で再生すると
> シンセSEのプリウォームも再生も走らない**（呼び出し側が null チェックしているので落ちはしない）。

- 上端中央 `x[-150..150] y[214..292]`（800x600基準）。`設定`(x≤-290) / 敵パネル(y≤180) / セリフ枠(y≤160) と非重複
- 総額0のときは自動で非表示。`SetVisible()` で盤面と一緒に隠せる（額は保持）
- 本編にも接続済み：`OnBettingCompleteFromServer` で積み増し、`OnScoreSettlementComplete` で0に。
  流局ではクリアしないので持ち越しがそのまま出る
- インスペクタ未設定でも簡易ラベルを自動生成する保険あり（`BuildFallbackLabel`）

## 16. HPメーターの頭打ちを修正

分母を「到達最高HP」まで広げる（`PlayerInfoUI` / `EnemyInfoUI` の `hpPeak` / `MeterMax`）。
33000 → 15000 の下落がメーターに出るようになった。

**注意**: `TutorialManager.ApplyHpToUI` は毎回 `SetMaxHP` を呼ぶので、`SetMaxHP` では分母を
引き下げない実装にしている。対局開始で引き直したいときは `ResetHpMeter(max)` を使うこと
（`GameUIPhaseController.OnGameStarted` と `StartTutorialFrom` から呼んでいる）。

瀕死ビネットとダメージSEの判定は「絶対量」のままにした（開始HP基準）。

## 17. 演出の軽量化（実測ベース）

「スキル演出が重い」の原因は描画ではなく **SEのプロシージャル生成** だった。実測値:

| 対象 | 修正前 | 修正後 |
|---|---|---|
| 透視SE（Sine+Noise 0.55s） | **4.11 ms**（毎回） | 0.70 ms → 以降 0.016 ms |
| ダメージSE（Saw+Noise） | **2.89 ms**（毎回） | 0.10 ms |
| 特殊勝利SE（0.9s） | **6.18 ms**（毎回） | プリウォーム済み |
| カットイン生成 | 0.66 ms（2回目以降） | 変更なし |
| 画面揺れ（Canvas移動） | 0.017 ms/フレーム | 変更なし（十分軽い） |
| 描画コール | +6（演出中） | 変更なし |

`AudioSynth` は **Noise を含む音をキャッシュ対象外**にしていたため、鳴らすたびに
48kHz×0.55秒ぶんを生成し、しかも**生成した AudioClip を破棄していなかった（リーク）**。

- ノイズ入りも数バリアント（既定2）だけ作って使い回す
- **プレイ中は生成しない**（未生成のバリアントに当たったら既にあるものを代用）
- キーは周波数25Hz・長さ10ms刻みに丸める（ダメージ音は連続値で呼ばれるためキャッシュが効かず、
  かつキャッシュが無限に増えていた）
- 上限 64 件を超えたら古い順に `Destroy`
- `AudioManager.PrewarmSynthSounds()` を `Awake` から呼び、16キー28本を先に生成（**58.6 ms / ロード時1回**）

> **PlayDamageSE / PlayHitSE の式を変えたら `PrewarmSynthSounds` の式も合わせること。**
> ずれるとプリウォームが効かず、初回だけ 2〜3ms のスパイクが戻る。

`PhaseTransitionUI` の空の `Update()` を削除。

### 塗り面積（フィルレート）側の削減

`PhaseTransitionUI` のインスペクタに項目を追加。既定値を軽い側にしてある。

| 項目 | 既定 | 元の挙動 |
|---|---|---|
| `splatterCount` | 2 | 固定2枚 |
| `splatterSizeMin/Max` | 500〜800 | 600〜1200（800x600基準で画面の4倍面積） |
| `portraitShadow` | false | 常にON（立ち絵の四角を丸ごと複製） |
| `textThemeShadow` | false | 常にON（黒影と合わせて Shadow 2枚） |

黒影は読みやすさのため常に付く。同条件A/Bの実測（800x600・全要素を画面内に置いた状態）:

```
修正前: Shadow 3枚 / 半透明で塗る面積 9.64 画面ぶん
修正後: Shadow 1枚 / 半透明で塗る面積 8.06 画面ぶん  （-16%）
```

> この面積は**軸平行バウンディングボックスの合計**なので、回転した血飛沫や透明余白の多い
> 立ち絵を過大に数えている。実際のGPU負荷はこれより小さい。相対比較の目安として使うこと。

戻したいときはインスペクタで ON にすれば元の見た目になる。

### 次に効きそうな未対応：フォントアトラスが3枚に溢れている

`Assets/Resources/PixelMplus10_DynamicFixed.asset` は **サンプリング pointSize = 90 / padding 9**。
グリフ1枚あたり約 11,600px² 使うため、**223グリフで 1024x1024 のアトラスを3枚**消費している。

TMP は「1つのテキストの文字が複数アトラスに散る」と**アトラスごとに描画コールが増える**。
実際、カットインの文字に `TMP SubMeshUI [... Atlas 2]` の子が付いていた（＝1テキストで2ドローコール）。

- 対策：pointSize を 45〜48 まで下げて Clear Dynamic Data → 全グリフが1枚に収まり、
  各テキストが1ドローコールになる。グリフ生成コストも約1/4
- ただし**全テキストの見た目（SDFの太り方）に影響する共有アセット**であり、
  かつ現在 git で未コミットの変更が乗っているので、勝手には触っていない
- 補足：動的アトラスはプレイ中に成長してアセット自体が dirty になる。
  git で font の `.asset` が勝手に変更されて見えるのはこれが原因

### カットイン初回の 6.2ms について（対応不要）

初回だけ 6.2ms、2回目以降 0.71ms。パーツ（スプライト・TMP・Canvas）を先に温めても消えず、
`ResetVisuals` も 0.10ms なので、**コルーチン本体の Mono JIT コンパイル**が原因と判断した。
ビルド先は WebGL（IL2CPP / AOT）なので**実機ビルドでは発生しない種類のコスト**。
エディタでの体感を根拠に演出を作り替えないこと。

### 計測方法のメモ

- `manage_profiler get_frame_timing` は**エディタ非フォーカスだと 0.2ms 等の当てにならない値**を返す
- 代わりに `UnityEditor.UnityStats.drawCalls / batches / setPassCalls` と `Stopwatch` を
  `execute_code` から使うのが確実（`Assets/../scratchpad` の bench_*.cs 参照）
- `execute_code` は CodeDom（C# 6）で動くので、ローカル関数・補間文字列の一部は使えない
- **Edit モードでコルーチンを起動すると生成物が消えずにシーンに残る。** 計測後は必ず
  `DeathGameCutinContainer` / `CutinRoot` を消すこと

## 19. セリフを送る前に牌が触れてしまう問題

第1局で「まずは山牌から好きな13枚を選んで」のセリフを送る**前**に盤面が出ていたため、
OKを押さないまま牌をクリックできてしまっていた。対策は2つ入れた。

1. **プレイヤーが自分で牌を選ぶ局（`allowManualHandSelection == true` = 第1局・第5局）は
   `revealBoardAfterLineIndex = -1`**（イントロを全部送り終えてから盤面を出す）。
   第1局は元々 2 で、最後の行＝指示のセリフが盤面表示より後に来ていた。
   行を足しても壊れないよう、インデックス指定ではなく `allowManualHandSelection` で
   まとめて -1 にしている（`BuildDefault()` の末尾）
2. **`PlayLines` の送り待ち中は `OnTryMoveTile` が黙って false を返す**（`_isWaitingForLine`）。
   盤面が出ている状態でセリフが流れる場面（`onHandFilledLines` など）でも同じ事故が起きるため。
   山牌→手牌・手牌→山牌の両方が `OnTryMoveTile` を通るので1箇所で塞げる

> ここで `ShowInterruptMessage` を出してはいけない。`DialogueUI` の本文を上書きして
> 送りボタンの行が消え、進行が止まる（§12 の注意と同じ罠）。黙って弾くこと。

第2局・第4局は `allowManualHandSelection = false` なので盤面は今も1行目のOK後に出る
（『自動』ボタンしか使わない局なので、牌を触っても従来どおり弾かれる）。

## 20. 第5局は自力で組ませる（誘導なし・両ボタン）

`TutorialRoundData.freeHandBuilding`（新規）を第5局だけ true にした。

- 13枚そろったら **『自動』と『決定』の両方**を出す（`HandButtonStage.AutoAndDecide`）
- **矢印・マスクの誘導は一切出さない**。`ApplyMockAutoMangan` の決定ボタンへの誘導、
  決定を弾いたときの自動ボタンへの誘導も `freeHandBuilding` の間はスキップする
- `requireAutoManganToConfirm` は**残している**（制約『満貫手以下での開始は不可』の担保）。
  自力で台本の手を組めていれば決定を押した時点で通るので、自動を押す必要はない

**`OnTryCompleteHandSelection` で満貫手の判定をやり直すようにした。**
元は「13枚そろった瞬間の1回だけ」判定していたので、そのあと牌を入れ替えて正しい手に直しても
決定が通らないままだった（自力で組ませる局では詰む）。

打牌フェイズに入った直後のセリフ（`onBattleStartLines`）を4行追加。
「賭けているのは金じゃなく血」という命懸けの導入にしてある。

## 21. 賭け金の支払いで体力が減るようにした（§14 の更新）

賭け金フェイズを終えた時点で、**両者の血から賭け金を引く**（`PlaceBet`）。
決着時の増減は §14 の式のまま。したがって:

- **満貫（1倍）で上がった勝者は差し引き0**（払った賭け金と同額が戻るだけ）
- 敗者は「払った賭け金」＋「賭け金×相手の倍率」の二重で減る

`BetPotUI` の「場の血」は**自分の賭け金＋相手の賭け金**なので、
各自2000なら 4000 と出る（自分2000 / 相手2000 の内訳も同じパネルに出ている）。
体力が引かれるようになったので、両者のHPが2000ずつ減って合計4000という対応が目で追える。

**賭け金は賭け金UIが確定した額を使う**ようにした（`_lastConfirmedBet`）。
以前は `data.betAmount` をそのまま積んでいたので、UIの表示額と場の額がずれる余地があった。

### 賭け金を組み直した（支払いで死ぬため）

支払い分が増えたので、元の額（2000/1000/1000/2000/2000）だと第5局の賭け金支払いで
相手が死んで決着演出が成立しない。**2000 / 600 / 600 / 1000 / 1000** に変更。
Python と実データ（`BuildDefault()` + `GameRules`）の両方で通し検証済み:

```
開始                                          P20000 / E20000
第1局 各-2000 → P18000/E18000
       跳満1.5倍 自分+3000 相手-3000          P21000 / E15000
第2局 各-600  → P20400/E14400  流局（持ち越し）P20400 / E14400
第3局 各-600  → P19800/E13800（賭け金は各1200）
       役満4倍×単騎2倍 相手+4800 自分-9600    P10200 / E18600
第4局 能力コスト-12700 → E5900
       各-1000 → P9200/E4900
       満貫1倍 自分+1000 相手-1000            P10200 / E 3900
第5局 各-1000 → P9200/E2900
       ダブル役満8倍 自分+8000 相手-8000      P17200 / E    0（決着）
```

**制約が1つ増えた: 賭け金の支払いで誰も死なないこと。**
特に第4局（能力コスト12700の直後）と第5局は相手の血が薄い。

## 22. チュートリアル最後の「相手が倒れる」演出

第5局の outro（きゃあああ！）のあと → 倒れる演出 → 吹き出しに「…………」→ タイトルへ。

### `EnemyInfoUI.PlayDeathRoutine()`（新規）

顔（`faceRenderer`）を消して、体（`characterRenderer` = ルートの `キャラ`）を下へ落とす。
落下は `p*p` で加速させている（等速だと沈むように見えて力が抜ける）。

- **まばたき・表情差し替え・バウンドのコルーチンを先に全部止める。**
  止めないと落下中に顔が戻ったり、バウンドが体を元の位置へ引き戻す
- インスペクタ調整可：`Death Fall Distance`（既定24）/ `Death Fall Duration`（既定1.2秒）
- 立ち絵は高さ約17.65・中心 y=0 の SpriteRenderer。Play モードでの実測:
  20 だと上端が viewport −0.04（ぎりぎり）、**24 で −0.18** になり確実に見えなくなる
- 顔は `キャラ/顔` の子スプライト。体を動かせば一緒に付いていく（顔は enabled=false で消す）

### `TutorialManager.RunEnemyDeath()`（新規）

`ScenarioRoutine` の全局終了後、`_enemyHp <= 0` のときだけ実行する。
盤面（手牌・山牌・河・HPパネル）を `SetBoardVisible(false)` で先に片付けてから落とす
（牌が残ったまま立ち絵が落ちると何が起きたか分からない）。

### `endingLines` を「…………」1行にした

倒れた直後に先輩の「チュートリアル終了だよ！」が出ると空気が壊れるので、
**元の先輩2行は削除**して沈黙だけにしてある。話者は既定なので吹き出しに `「…………」` と出る。
これを送るとタイトルへ遷移する。戻したい場合は `BuildDefault()` の `s.endingLines` を編集。

## 18. その他

- `MatchMomentumUI` の namespace を `KillingMahjong.UI.Effects` に統一（`GameUIManager` の参照も追随）
