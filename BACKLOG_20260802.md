# 要望リスト整理（2026-08-02 受領・24件）

プランナーからの要望を、性質ごとに分類して着手順を検討するための表。
**「確認したいこと」がある項目は、着手前に回答をもらうこと。**

---

## A. すぐ直せる（小・低リスク）

| # | 内容 | 対象 | メモ |
| :--- | :--- | :--- | :--- |
| 1 | 体力ゲージをもう少し左へ | シーン | **済** `EenemyInfoUI/EnemyPanel` を両シーンで `(-130,95)→(-190,95)` |
| 4 | 「場の血」を消す | シーン | **済** `BetPotUI.showPot = false`。額の積算は残してあるので戻せる |
| 5 | Auto ボタンを消す | シーン/UI | **済** `HandUI.showAutoDiscardButton = false`（隠すだけ。機能は `AutoDiscardController` に残存） |
| 12 | タイトルに戻ったら BGM を止める | コード | **済** `AudioManager` が `sceneLoaded` を購読。戻り道が4通りあるため遷移側ではなくここで止める |
| 17 | 手牌フェイズで YOUR TURN が出るのがおかしい | コード | **済** `TutorialManager.SetBoardVisible` に打牌フェイズ判定を追加 |
| 21 | 役一覧が2行になっているのを直す | シーン | **済** `役アイテムPrefab` を `NoWrap` + 自動サイズ 8〜13 |

## B. 演出・見た目（中）

| # | 内容 | メモ |
| :--- | :--- | :--- |
| 2 | ドラの光り方が見づらい | 先日入れた `DoraShine` の調整。色・大きさ・頻度 |
| 3 | OK ボタンが地味。**吹き出しを押して送る**形にしたい | 入力方式の変更。15 と同じ方向 |
| 7 | セリフで危機を煽りたい | セリフ追加。CSV とチュートリアル台本の両方 |
| 22 | 役強化が3つ以上あるとき、それを示したい | 常時役一覧は3枠固定。溢れたときの表現が必要 |

## C. レイアウトの統一（要まとめて実施）

| # | 内容 | 状況 |
| :--- | :--- | :--- |
| 23 | **自分を右・相手を左に統一したい** | **済**（下記） |
| 13 | 「場の血」をやめ、**総獲得のゲージ**にする。賭けた額のUIが、勝つとゲージに吸収される演出 | **済**（下記） |

### 23 の内訳（2026-08-02 済・両シーン）

洗い出した結果、逆になっていたのは2つだけだった。

| 対象 | 前 | 後 |
| :--- | :--- | :--- |
| `常時役一覧` の `自強化1〜3` | x = -55（左） | **x = +55（右）** |
| `常時役一覧` の `敵強化1〜3` | x = +55（右） | **x = -55（左）** |

もともと正しかったもの（変更なし）:

- 自分HP `PlayerInfoUI/HPPanel` x = +317（右）／敵HP `EenemyInfoUI/EnemyPanel` x = -191（左）
- 準備完了 自 x = +285（右）／敵 x = -100（左）
- 獲得ゲージ 自＝右（青）／敵＝左（赤）
- 相手の待ち牌 `EnemyWaitUI/待ち牌Panel` x = -315（左）

**自分の待ち牌パネルは左下のまま**（x = -330）。
いったん右下へ動かしたが、左下でよいとのことで戻した。左右統一の例外。
- 相手の待ち候補 `WaitDeductionUI` x = -292（左）
- 役チップの色 自＝青／敵＝赤（ゲージの色と対応）

河・手牌・山牌は上下（敵＝上／自分＝下）なので対象外。

**13 は 4・23 とセットで考えるべき。** 個別に動かすと二度手間になる。
数字はゲージ内に白文字で表示し、0点のときも表示する。

### 13 の進捗（2026-08-02）

**実装済み** — `Assets/Scripts/UI/ScoreGaugeUI.cs`。共有いただいたモック画像に合わせてある。

- 画面上部を横切る**1本の帯**（幅400px）。**中央の王冠が 30000 ＝勝利ライン**
- 左半分が相手（赤）、右半分が自分（青）。それぞれ外端から中央へ伸びる
- 数字は帯の中に白文字。0点でも表示
- 帯の下に**賭け金のフィールド**（104x24px、外枠が持ち主の色、数字は白）。**常時表示、0でも「0」**
- 決着すると**枠は動かず、中の数字だけ**が勝者側ゲージの中央へ飛ぶ（0.45秒、縮小＋フェイド）。
  吸い終わってからゲージが伸びる（0.6秒）
- 局の決着で `liquidation.winner_gain` を足し込み、`累計 / 30000` の比率で伸ばす
- 積み込みは `GameUIPhaseController.OnScoreSettlementComplete`。チュートリアルでは積まない
- **勝敗の判定はしていない**（判定はサーバーの担当という取り決めどおり）
- 重なり回避のため `常時役一覧` を縮小・移動（scale 0.85→0.62、pos (215,170)→(300,150)）

サーバー側へは `SERVER_REQUESTS_20260801.md` の **A-5** として要望を出した
（累計値・目標値・決着理由を送ってほしい）。それまでは自前の足し込みで動く。

**未確認**：実際のロンで決着したときの通しの動作。
手で `AbsorbStakesIntoGauge` を呼んで部分的に見ただけ。

## D. 不具合（要調査）

| # | 内容 | 状況 |
| :--- | :--- | :--- |
| 6 | 牌交換がおかしい？ | **8/1 に2回修正済み**（index 取り違え・二重適用）。それでも残る症状か、別件かを確認したい |
| 9 | ロン後、相手の手牌がぐちゃぐちゃのまま | 未調査 |
| 10 | 透視された牌はマリガンできないはずが、できてしまう | **仕様の所在を確認したい**（サーバーが弾くべきか、UIで抑止か） |
| 11 | 「選びなおす」で全リセットされる | 一部だけ戻すのが期待動作か要確認 |
| 20 | チュートリアル1局目が「注連」？ | 内容が読み取れず。詳細を聞く |

## E. 大きめの変更（設計が要る）

| # | 内容 | メモ |
| :--- | :--- | :--- |
| 8 | 「今まで何点獲得したか」の UI | **13 のゲージで満たせているはず。** 別途必要なら仕様を聞く |
| 14 | **Firefox 非対応の解消** | WebGL の互換性問題。原因調査から。**影響が大きく、単独で時間を取る必要あり**。症状の聞き取り待ち |
| 18 | 手牌決定 UI の変更 | **ほぼ済**（下記）。残りは待ち牌が多いときのスクロール |
| 19 | 自分の手牌は普段非表示。**ボタンを押している間だけ**オーバーレイ表示 | 入力とUIの両方に関わる。未着手 |

### 18 の進捗（2026-08-02）

- ランクから「以上」を削除（`跳満以上` → `跳満`）
- 確認ダイアログは**牌だけ**を並べる。役名は並べない
- **牌にカーソルを合わせると**、選んだ13枚＋その牌と役名・ランクをオーバーレイ表示
  （`ConfirmationDialogUI` の `WaitInfo` / `ShowHandPreview`）

**未実装**：待ち牌が多いときのスクロール。今は6枚超で 0.6 倍に縮小して並べている。

## F. チュートリアル

| # | 内容 | 状況 |
| :--- | :--- | :--- |
| 15 | ロンボタン／選びなおし／クリックで次へ | **済**（下記） |
| 16 | 役強化で「清一色」を強化するようにする | **元から「清一色」だった。**コード既定値・アセット・適用先すべて確認済み |
| 20 | 1局目の件（D にも記載。内容確認待ち） | 未着手 |

### 15 の内訳（2026-08-02 済）

- ロンボタン → 対局と同じ `RonWaitPanel` を出す。表示処理を `GameUIManager.ShowRonWaitPanel()` に
  切り出して対局・チュートリアルの両方から通す（以前は `AgariSelectionUI` で別物だった）
- 「選びなおす」→ `HandUI.UpdateLayout` に `!IsTutorialMode` を追加して非表示
- セリフ送り → **画面のどこをクリックしても進む**（`DialogueUI.ShowAdvanceOnAnyClick`）。
  右下のOKボタンは出さず、吹き出しの右下で「▼」が点滅する。**これは要望3も兼ねている**

### チュートリアルにもゲージを追加（2026-08-02）

チュートリアルはサーバーに繋がっていないので `TutorialManager` の値から流し込む。

- `ApplyPotToUI()` が賭け金を `ScoreGauge.SetStakes()` にも渡す
- `RunPlayerRon` / `RunEnemyRon` が**ロン演出のあと**に `AbsorbStakesIntoGauge()` を呼ぶ。
  演出中は盤面ごと隠れるので、前に呼ぶと見えない間に伸びきってしまう
- `常時役一覧` の縮小・移動を OpeningScene にも適用

## G. 進め方について

| # | 内容 |
| :--- | :--- |
| 24 | サーバー側へ送ってほしい情報があれば、すぐ言語化して要望する |

→ **すでに `SERVER_REQUESTS_20260801.md` として作成済み。** 今後も気づいた時点で追記する運用にする。

---

## 回答待ち

1. **#6 牌交換** … 8/1 の修正後も再現するか。する場合の具体的な症状
2. **#10 透視とマリガン** … サーバーが弾く仕様か、クライアントで抑止する仕様か
3. **#11 選びなおす** … 何が戻り、何が残るのが正しいか
4. **#20** … 「注連」の意味
5. **#14 Firefox** … 症状（起動しない／描画が崩れる／音が出ない等）
6. **#16** … 別の役が表示されていたなら、実際に見えた役名

## 残りの着手順（推奨）

1. **D群の調査（9）** … 聞き取り不要。こちらだけで進められる
2. **#23 自分右・相手左の統一** … ゲージで左右を決めたので、今なら揃えやすい
3. **B群（2・7・22）** … 演出とセリフ
4. **D群の残り（6・10・11・20）** … 回答が来次第
5. **E群（19・18のスクロール・14）** … 設計が要るもの

---

## 実機で未確認のもの

- ロンで決着したときの通しの動作（賭け金の吸収 → ゲージが伸びる）。対局・チュートリアルとも
- チュートリアルのロンボタン・クリック送り・役一覧の縮小
- 手牌決定ダイアログのオーバーレイ（背景を不透明にした後の見た目）
- `PixelBloodEffect` / `BloodTransferEffect` / `DoraShine` / `YakuTooltip` / `MenuButtonHover`
