# 引き継ぎメモ（2026-08-02）

再起動をまたいで作業を再開するための覚え書き。
詳しい経緯は `BACKLOG_20260802.md`、サーバーへの依頼は `SERVER_REQUESTS_20260801.md`。

---

## 再開時の状態

- Unity：**`タイトルシーン` を開いた状態**（タイトル画面を作業したため）。
  対局まわりを触るときは `UIテストシーン` に戻すこと。
  **Play は停止、シーンは保存済み、コンパイルエラー 0**
- 変更はすべてディスクに保存済み。**コミットはしていない**
- 対戦ボットは停止済み。必要なら
  `python opponent_bot.py`（スクラッチパッド内。認証トークンはシーンから読み出す作り）

---

## 今回入れた変更

### 獲得ポイントのゲージ（要望13・4・8）

`Assets/Scripts/UI/ScoreGaugeUI.cs`（新規）。共有されたモック画像に合わせてある。

- 画面上部を横切る**1本の帯**（幅400px）。**中央の王冠が 30000 ＝勝利ライン**
- 左半分が相手（赤）、右半分が自分（青）。それぞれ外端から中央へ伸びる
- 数字は帯の中に白文字。**0点でも表示**
- 帯の下に**賭け金のフィールド**（104x24px、外枠が持ち主の色、数字は白）。**常時表示、0でも「0」**
- 決着すると**枠は動かず、中の数字だけ**が勝者側ゲージの中央へ飛ぶ（0.45秒、縮小＋フェイド）。
  吸い終わってからゲージが伸びる（0.6秒）
- 王冠は 11x8 のドット絵をコードで生成（黒フチ焼き込み）。
  TMPの既定フォントに `♛` が無く化けたため
- 積む値は **`liquidation.winner_gain`＝賭け金 × 役の倍率**。
  倍率は満貫1倍／跳満1.5倍／倍満2倍／三倍満3倍／役満4倍／ダブル役満8倍
- **勝敗の判定はしていない。** 判定はサーバーの担当という取り決めどおり

呼び出し口：

| 場所 | 処理 |
| :--- | :--- |
| `GameUIPhaseController.OnBettingCompleteFromServer` | `AddStakes()` |
| `GameUIPhaseController.OnScoreSettlementComplete` | `AbsorbStakesIntoGauge()` |
| `GameUIPhaseController.OnGameStarted` | `ResetScores()` |
| `TutorialManager.ApplyPotToUI` | `SetStakes()`（チュートリアル用） |
| `TutorialManager.RunPlayerRon` / `RunEnemyRon` | ロン演出の**後**に `AbsorbStakesIntoGauge()` |

**「場の血」（BetPotUI）は `showPot = false` で非表示。** 額の積算は残してあるので戻せる。

### チュートリアル（要望15・16）

- ロンボタン → 対局と同じ `RonWaitPanel` を出す。表示処理を
  `GameUIManager.ShowRonWaitPanel()` に切り出して両方から通す
  （以前は `AgariSelectionUI` で本編と別物だった）
- 「選びなおす」→ 非表示（`HandUI.UpdateLayout` に `!IsTutorialMode`）
- セリフ送り → **画面のどこをクリックしても進む**（`DialogueUI.ShowAdvanceOnAnyClick`）。
  右下のOKボタンは出さず、吹き出しの右下で「▼」が点滅する。**要望3も兼ねている**
- 役強化の「清一色」は**元から清一色だった**（コード既定値・アセット・適用先すべて確認済み）

### 手牌決定UI（要望18）

- ランクから「以上」を削除（`跳満以上` → `跳満`）
- 確認ダイアログは**牌だけ**を並べる。役名は並べない
- **牌にカーソルを合わせると**、選んだ13枚＋その牌と役名・ランクをオーバーレイ表示
  （`ConfirmationDialogUI.WaitInfo` / `ShowHandPreview`）

### 左右の統一（要望23）

- `常時役一覧` の `自強化1〜3` を右（x +55）、`敵強化1〜3` を左（x -55）へ
- `役アイテムPrefab`（全役一覧）の数値も同様に入れ替え（自 x90 / 敵 x73.7）
- **自分の待ち牌パネルは左下のまま**（x -330）。いったん右下へ動かしたが戻した。左右統一の例外

### 役強化が3つ以上（要望22）

`YakuListUI.UpdateActiveBoosts`。枠（3つ）より多いときは**最後の枠を「+3」のような件数**に使う。
あわせて**翻数の大きい順に並べ替える**（`Dictionary` の順は保証されないため）。

### ドラのきらめき（要望2）

`Assets/Scripts/UI/DoraShine.cs`。詳細と数値の根拠は `BACKLOG_20260802.md` の「2 の進捗」。

- **牌からはみ出す大きさに**（`sizeRatio` 0.95 → 1.4）
- **トゲを先細りの針に**。太さ一定で先端が四角い塊になっていたのを直した
- **対角のトゲを追加**（コメントには書いてあったが実装が無かった）
- **芯を白・外を金**にして、明度差を絵自体に持たせた
- 頻度 2.6秒 → 1.6秒
- **バグ修正**：`OnEnable` で牌の実寸を測っており、レイアウト未確定だと
  牌の大きさに関係なく固定 40px になっていた。光る直前に測り直すようにした

- **バグ修正**：牌のプレハブには Canvas 配下でない SpriteRenderer 版があり、
  そこでは見えない子 Image を作るだけだった。`GetComponentInParent<Canvas>()` で弾くようにした

このコンポーネントは**シーンに置かれておらず** `TileVisual` が `AddComponent` するだけなので、
見た目を変えるときはコードの既定値を触る（シーン側に上書きは無い。GUIDで確認済み）。

**Play モードで実機確認済み。** 確認は本番の対局を待たずに、`execute_code` で
テスト用 Canvas に `イーピンUI.prefab` を7枚並べて `DoraShine` を付ければ数秒でできる
（スクラッチパッドの `setup_test.cs`）。配牌に10〜35秒かかるので対局を回すより速い。

### 手牌の覗き見（要望19）

打牌フェイズで手牌を普段は隠し、**「手牌」ボタンを押している間だけ**暗幕付きで前面に出す。
詳細は `BACKLOG_20260802.md` の「19 の進捗」。

- 新規 `HoldButton.cs`（押している間だけを扱う部品）＋ `HandUI` に制御を追加
- **ボタンは実行時に生成**。対局シーンが2つあるので、シーンに直接置かない
- **チュートリアルでは無効**。`hideHandUntilPeek` を false にすれば元の常時表示に戻る
- 打牌フェイズで捨てるのは山牌で、手牌は選択対象ではないため隠しても操作を妨げない
- 覗いている間は**画面下部の中央へ寄せて1.3倍に拡大**（`PeekOverlayPos = (0,-165)`）。暗幕は無し
- **調整値は `SerializeField` にしていない。** シーンに焼き付いてコード側の変更が
  効かなくなるうえ、対局シーンが2つあるため。`HandUI.cs` 先頭の定数を直接触る

**実機確認済み。** 本番サーバーで対局を回し、隠れる→押して出る→離して戻るまで確認した。

**踏んだ落とし穴は全部「非アクティブなうちに設定しても効かない」が根。**
`overrideSorting` が入らず手牌が山牌に隠れる、牌の範囲が測れず画面外へ飛ぶ、など。
`SetPeek` は「表示 → 重なり順 → 位置合わせ」の順にしてある。詳細は `BACKLOG_20260802.md`。

**対局の検証は人が操作しなくてよい。** MCP の `execute_code` で
`Auto` → `Decide` → 確認ダイアログ `OK` → 賭け金 `Confirm` の各 `onClick.Invoke()` を
順に呼べば打牌フェイズまで進む。配牌の40秒だけは待つ。

### タイトル画面の作り直し（「じゃんぱいあ」）

`Assets/Scenes/タイトルシーン.unity` を組み直した。構図はウディコン18の
**「あくま1/2」**（キャラを大きく左へ寄せ、右上に装飾ロゴ、右下にメニュー）を参考にしている。

作業は**エディタ拡張に落としてある**ので、何度でもやり直せる:
`Tools > UI > タイトル画面を組み直す（じゃんぱいあ）`
（`Assets/Scripts/Editor/TitleScreenBuilder.cs`。数値は先頭の定数だけ触ればよい）

| 追加・変更 | 内容 |
| :--- | :--- |
| `TitleLogo` | 「じゃんぱいあ」。PixelMplus に縦グラデーション（白→深紅）＋輪郭＋落ち影 |
| `Assets/Resources/Title/JanpaiaTitle.mat` | ロゴ専用の TMP マテリアル。共有マテリアルを触ると他の文字まで太る |
| `Assets/Resources/Title/TitleSparkle.png` | 4方向スター。ビルダーがコードで生成する |
| `TitleScrim` | 右へ行くほど暗くする幕。`HorizontalGradient`（新規）で頂点色を左右で変える |
| 女の子 | 拡大 1.55・位置 (-215,-150) で画面外へ切れさせる。白フチも同じ値×1.03 |
| メニュー | 黒い長方形をやめて文字だけに。左に深紅の菱形の印 |

**気をつけた点:**

- **ボタンは作り直していない。** `onClick` が `TitleUIManager` へシリアライズ済みなので、
  作り直すと配線が消えて「押しても何も起きない」状態になる。位置と見た目だけ変えた
- 当たり判定を残すため、ボタンの `Image` は消さずに**透明**にしている
- キャラを大きくすると `ボタン達` に被る。**兄弟の順番**を
  背景 → 幕 → 白フチ → 女の子 → ロゴ → メニュー に並べ直している

**踏んだ落とし穴（同じことをするとき用）:**

- `tmp.font` を指定しないと TMP 既定の **LiberationSans** になり、ひらがなが全部豆腐になる。
  フォントはパス決め打ちではなく**既存のメニュー文字から借りて**いる
  （`PixelMplus10_DynamicFixed` という名前のアセットが複数あるため）
- `TextureImporter.textureType = Sprite` **だけでは Sprite のサブアセットが作られない**。
  `spriteImportMode = Single` も要る。忘れると `LoadAssetAtPath<Sprite>` が null を返し続け、
  `Image` が既定の**白い四角**を描く
- PixelMplus は線が細いので、輪郭 0.30 だと文字の面が食い潰されてほぼ黒い塊になる。
  `FaceDilate 0.10` で面を太らせ、輪郭は 0.12 に留めた
- `EditorUtility.CopySerialized` は**名前まで複製する**。マテリアル名を戻さないと
  インスペクタで元のフォントマテリアルと見分けが付かない

**見つけた既存の不具合（未対応・仕様判断が要る）:**

**「ソロ」ボタンに `onClick` が設定されていない**（listeners=0）。押しても何も起きない。
「マルチ」だけが `OnClickStartButton` を呼んでいる。`TitleUIManager` には
ソロ／マルチを出し分けるメソッドが無いので、どう分けるかを決める必要がある。

### 不具合修正（要望9）

**ロンで決着したとき、相手の手牌が打牌フェイズのまま（ツモ順でバラバラ・大半が伏せたまま）だった。**
並べ直して公開する処理が流局（`HandleDraw`）にしか無かった。
`OnScoreSettlementComplete` に同じ処理を追加。

### レイアウト（要望1・5・12・17・21）

| # | 内容 |
| :--- | :--- |
| 1 | `EenemyInfoUI/EnemyPanel` を `(-130,95)→(-190,95)` |
| 5 | `HandUI.showAutoDiscardButton = false`（隠すだけ。機能は `AutoDiscardController` に残存） |
| 12 | `AudioManager` が `sceneLoaded` を購読してタイトルでBGM停止（戻り道が4通りあるため） |
| 17 | `TutorialManager.SetBoardVisible` に打牌フェイズ判定を追加 |
| 21 | `役アイテムPrefab` を `NoWrap` + 自動サイズ 8〜13 |
| — | `常時役一覧` を scale 0.85→0.62、pos (215,170)→(300,150)（ゲージとの重なり回避） |

---

## サーバーへの依頼（`SERVER_REQUESTS_20260801.md`）

今回 2件追加した。**Python 側は他の人の担当なので触っていない（読むだけ）。**

### A-5 獲得点30000で勝ち、という勝利条件

HP0の決着は残したまま、**獲得の累計が30000で勝ち**を足したい。判定は局の終わり。
`liquidation` に `winner_total_gain` / `loser_total_gain` / `target_gain` を入れてほしい。
`game_end` に決着理由（`hp_zero` / `target_gain`）も。
それまではクライアントが自前で足し込む。

### A-6 配牌を打牌フェイズ中に仕込んでほしい

**局が変わるたびに10〜35秒待たされる。** 原因は `TileWall.deal()` が
「満貫以上の聴牌形が出るまでシャッフルして引き直す」方式で、これを人数ぶん直列で回しているため。
実測 21.2秒 / 34.6秒 / 12.4秒。

配牌は直前の局の結果に一切依存しない（`self.tile_wall` を触るのは `_deal_tiles()` の中だけ）ので、
**打牌フェイズ中に次局ぶんを裏で計算しておけば待ち時間を隠せる。**

**クライアントからは短縮できない。** サーバーに配牌を指示するアクションが存在せず
（`status / is_tenpai / skill / select / select_cancel / select_confirm / bet / discard / agari / next_round`）、
`_deal_tiles()` へ至る道は「両者の next_round」だけ。

---

## これからやること

### すぐ着手できる

| # | 内容 |
| :--- | :--- |
| 7 | セリフで危機を煽る。CSV とチュートリアル台本の両方。**言葉のイメージが要る** |
| 18残 | 待ち牌が多いときのスクロール。今は6枚超で0.6倍に縮小して並べている |

### 回答待ち

| # | 聞きたいこと |
| :--- | :--- |
| 6 | 牌交換 — 8/1 の修正後も再現するか。する場合の症状 |
| 10 | 透視された牌のマリガン — サーバーが弾く仕様か、UIで抑止する仕様か |
| 11 | 選びなおす — 何が戻り、何が残るのが正しいか |
| 14 | Firefox — 起動しない／描画が崩れる／音が出ない、どれか |
| 20 | チュートリアル1局目の「注連」の意味 |

### 判断が要るもの

- **ゲージの伸びが遅い可能性。** 「賭け金 × 倍率」を選んだので、最小賭け（200）だと
  跳満でも300＝30000の1%しか進まない。テンポが気になるなら
  目標値を下げるか、賭け金の単位を上げる
- **「+3」が翻数と紛らわしいかもしれない。** 他の枠が「清一色+3」＝翻数なので。
  気になるなら `他3件` などに変えられる

---

## まだ一度も画面で見ていないもの

- `PixelBloodEffect` / `BloodTransferEffect`（ドットの血・血が流れる演出・赤い画面周）
- `DoraShine`（ドラのきらめき）
- `MenuButtonHover`（タイトルのホバー）
